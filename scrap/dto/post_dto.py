class PostDto:
    def __init__(self, title, url, dt, img_url, article):
        self.title = title
        self.url = url
        self.dt = dt
        self.img_url = img_url
        self.article = article